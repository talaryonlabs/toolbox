mode: ContinuousDelivery
branches:
  main:
    tag: ''
    increment: Patch
    prevent-increment-of-merged-branch-version: true
    regex: ^main$
  dev:
    tag: dev
    mode: Mainline
    increment: None
    regex: ^dev$
  feature:
    tag: feature
    increment: Inherit
    regex: ^feature?[/-]
    source-branches: [ main, dev ]